baseURL = "https://lex00.github.io/wetwire-azure-go/"
languageCode = "en-us"
title = "Wetwire Azure"

# Theme is added as git submodule in themes/wetwire
theme = "wetwire"

# Clean URLs
[permalinks]
  [permalinks.page]
    '/' = '/:slug/'
  [permalinks.section]
    '/' = '/:slug/'

# Syntax highlighting
[markup]
  [markup.highlight]
    style = "dracula"
    lineNos = false
    tabWidth = 4
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# Main menu
[menus]
  [[menus.main]]
    name = "CLI"
    pageRef = "/cli"
    weight = 10
  [[menus.main]]
    name = "Quick Start"
    pageRef = "/quick-start"
    weight = 20
  [[menus.main]]
    name = "FAQ"
    pageRef = "/faq"
    weight = 30
